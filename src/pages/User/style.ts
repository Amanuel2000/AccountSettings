import {
    createStyles,rem
  } from '@mantine/core';
export const useUserStyle = createStyles((theme) => ({
    order: {         
    maxWidth: rem(400),
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'flex-end',
    marginLeft: 'auto',
    marginRight: 'auto',
  
    }

    

}
  ))